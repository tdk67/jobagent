# JobAgent TASKBOARD — remediation loop (see notes/FIX_PLAN.md)

| Task | Scope | Status | Note |
|------|-------|--------|------|
| F1 | PII purge: sanitize test fixture + squash history + force-push | **done** | commit `04e93ae`, remote history verified CLEAN via fresh clone (2026-09-12, done by lead agent, not via loop) |
| F2 | Extension message-channel security (postMessage token/PII broadcast, unorigin-checked listener) | **done** | commit `4f6a0d6` [QA: PASS] — broadcast block + window listener deleted, apiToken removed from content payload, runtime-message path intact, 27 tests green |
| F3 | Gateway auth hardening (Host validation vs DNS rebinding, pair endpoint logging, Docker loopback bind) | **done** | commit `78d425f` [QA: PASS] — Host-header guard on all /api/* + /a2a/* (403 invalid Host), pair logs loopback client, Dockerfile/compose bind 127.0.0.1, run_agent env overrides A2A_HOST/A2A_PORT (CLI>env>cfg); 32 tests green |
| F4 | Correctness: city bug, IMAP date range, dead LLM fallback, Gmail no-op, Strands model fail-loud | **done** | commit `7ee5e90` [QA: PASS] — city extraction fix, build_imap_search_criteria + SINCE/BEFORE wiring, llm_fallback config flag, Gmail dead-adapter loud-warning guard, llm_available fail-loud gating; 42 tests green |
| F5 | Data integrity: no fabricated roles in AfA import path, remove auto-import magic, gate legal autofills | **done** | commit `8942d86` [QA: PASS] — UNKNOWN_ROLE placeholder (no overwrite of real roles), removed C:/Data auto-import magic, --import-summary requires path (exit 2), legal questions marked-for-review instead of Ja/Nein, role-less cover letter arg; 45 tests green (3 new) |
| F6 | Dead config wiring, duplicate @tool removal, 400 contract fix, README claims | **done** | commit `174a874` [QA: PASS] — env overrides wired (IMAP_HOST/PORT/USE_SSL, A2A_HOST/PORT, JOBAGENT_DB; env>files>defaults), 3 unused module-level @tool duplicates deleted (engine classes kept), unknown A2A action now HTTP 400 (HTTPException re-raised), .env.example pruned (no OPENAI/GMAIL_APP_PASSWORD), README test-count neutral + provider-inference privacy disclosure + env-override doc; 49 tests green (4 new) |
| F7 | Final regression & judge-readiness sweep (evidence only) | **done** | commit `080c05f` [QA: PASS] — suite green twice (49 passed x2, no flake), PII + secret scans clean across all history (scanner self-verified), --demo on /tmp copy exit 0 (1 interview / 1 rejection / 1 spam / 3 reports), server smoke on :8799 (health 200, evil Host 403, pair token, 401 unauth / 200 authed caps, killed after); evidence: notes/evidence/F7-qa.md |

Sequencing: strictly F2 → F3 → F4 → F5 → F6 → F7 (worker → QA gates commit+push → next).
