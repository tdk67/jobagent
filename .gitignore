# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments & Virtual Envs
.venv/
env/
venv/
ENV/

# Secrets & Private Personal Identifiers (PII) - STRICT NEVER COMMIT
*.local.json
profile.local.json
config.local.json
answered_questions.json
answered_*.json
.jobagent_token
.env
.env.*
!.env.example

# Local Application Data & Outputs
/data/
output/
/storage/
*.log
*.pdf
models/email_classifier/*.pkl

# Working Reference POCs (Local developer migration snapshots)
reference_poc/
scratch/

# IDE & OS Files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local agent workflow logs (worker/QA sessions, prompt files, PII pattern list)
logs/

# Video demo deliverables (output artifacts, not source)
video-demo/

# Sync runtime (backups, logs, staging) — never commit
sync/
!sync/.gitkeep
